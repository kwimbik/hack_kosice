<Window x:Class="CityPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CityPlanner"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <TabControl>
            <TabItem Header="City map">
                <Grid x:Name="grid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ToolBar>
                        <Button Content="Import map" Click="ImportMap_Click"/>
                    </ToolBar>
                    <Canvas x:Name="cMMap" Grid.Column="0">

                    </Canvas>
                    <Canvas x:Name="controls" Grid.Column="1">
                        <Button Grid.Column="1" Content="Button" HorizontalAlignment="Left" Margin="191,91,0,0" Grid.Row="1" VerticalAlignment="Top"/>
                    </Canvas>
                </Grid>
            </TabItem>
            <TabItem Header="Services">
            </TabItem>
        </TabControl>

    </Grid>
</Window>
